# === 하네스 로컬 컨텍스트 ===
.claude/
CLAUDE.md
docs/
.worktrees/
.atlas/

# === python ===
__pycache__/
*.py[cod]
.venv/
venv/
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# === IDE / OS ===
.vscode/
.idea/
.DS_Store
Thumbs.db

# === Graft 산출물 === (루트 한정 — src/anograft/bank 패키지와 겹치지 않게)
/out/
/bank/
/samples/
*.lock
!requirements-lock.txt

/runs/

# 학습기 등록(사람마다 경로가 다르다 — 예시는 trainers.example.yaml)
/trainers.yaml

# ultralytics 가 cwd 에 내려받는 가중치(yolov8n.pt 등, tools/train_mvtec_map.py)
/*.pt

# === 웹 UI (v0.10) ===
/web/node_modules/
# 빌드 산출물은 저장소에 두지 않는다 — CI 가 만들어 wheel·zip 에 싣는다(결정 6 ①).
/src/anograft/web/static/
